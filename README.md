# AI Image Processing

YOLO11 Segmentation を利用して画像内の指定オブジェクトへ自動でモザイク（ぼかし）を適用するツール。

## 使用技術

- Python
- Ultralytics YOLO11
- OpenCV

## 実行方法

```bash
pip install -r requirements.txt
python main.py


学習済みモデル（best.pt）は含まれていません。